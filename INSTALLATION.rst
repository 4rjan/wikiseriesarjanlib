============
Installation
============

At the command line::

    $ pip install wikiseriesarjanlib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wikiseriesarjanlib
    $ pip install wikiseriesarjanlib

Or, if you are using pipenv::

    $ pipenv install wikiseriesarjanlib

